name: Auto-add to project

on:
  issues:
    types: [opened]
  pull_request:
    types: [opened]

jobs:
  call:
    uses: wyre-technology/.github/.github/workflows/auto-add-to-project.yml@main
    secrets: inherit
