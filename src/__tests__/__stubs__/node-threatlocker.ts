// Stub for @wyre-technology/node-threatlocker — dist/ is absent until the SDK
// is built; this stub lets the test runner resolve the import without error.
export class ThreatLockerClient {
  constructor(public readonly creds: unknown) {}
}
